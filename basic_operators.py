def subtract(x, y):
            difference = x - y
            print(difference)
    
            return difference

print(subtract(x, y))


def multiply(x, y):
            return x * y
    
print(multiply(x, y))


def divide(x, y):
            quotient = y / x
            print(quotient)

divide(x, y)

    
def add(c, d):
            sum = c + d
            
            return sum

print(add(c, d))


def modulus(a, b):
            return b % a
    
modulus(a, b)


def exponent(a, b):
            result = a ** b
            
            return result

print(exponent(a, b))


def add_subtract(s, t, u):
            print(s - t+u)

add_subtract(s, t, u)

    
def multiply_divide(a, b, c):
            return b*c / a

print(multiply_divide(a, b, c))


def add_divide(a, b, c):
            return b/c + a

print(add_divide(a, b, c))


def subtract_multiply(a, b, c):
            return a - c*b

print(subtract_multiply(a, b, c))


            
            

    
